import { TenantTheme } from './tenant-theme';

describe('TenantTheme', () => {
  it('should create an instance', () => {
    const directive = new TenantTheme();
    expect(directive).toBeTruthy();
  });
});
